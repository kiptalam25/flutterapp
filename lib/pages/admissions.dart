import 'package:flutter/material.dart';

class Admissions extends StatelessWidget {
  const Admissions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Admissions"),),
      body: Center(
        child: Text("Admissions"),
      ),
    );
  }
}
